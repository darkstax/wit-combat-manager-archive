# Technical Constraints

- This repository is an archived Python snapshot.
- Prefer no feature work here; make only compatibility fixes, security fixes, or historical-reference edits.
- Use `code-review-graph` first when available.
- Keep behavior aligned with the active PySide6 project only when a backport is explicitly requested.
- Do not migrate architecture or dependencies here unless the user asks for an archive refresh.
